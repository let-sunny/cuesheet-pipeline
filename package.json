{
  "name": "cuesheet-pipeline",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "description": "Cuesheet-driven video assembly pipeline (schema / render / web)",
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=11"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:checks": "vitest run",
    "check:repo": "node scripts/checks/check-language.mjs && node scripts/checks/check-no-emoji.mjs && node scripts/checks/check-component-anatomy.mjs && node scripts/checks/check-test-selectors.mjs && node scripts/checks/check-schema-examples.mjs && node scripts/checks/check-agents-doc.mjs",
    "new:component": "node scripts/new-component/new-component.mjs",
    "episode": "node scripts/episode.mjs",
    "token-usage": "node scripts/token-usage-report.mjs",
    "e2e": "playwright test --config tests/e2e/playwright.config.ts",
    "e2e:ui": "playwright test --config tests/e2e/playwright.config.ts --ui"
  },
  "devDependencies": {
    "@astryxdesign/cli": "0.1.4",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.20.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
