{
  "name": "cuesheet-pipeline",
  "version": "0.0.0",
  "private": true,
  "description": "Cuesheet-driven video assembly pipeline (schema / render / web)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
