{
  "name": "@cuesheet/render",
  "version": "0.0.0",
  "description": "Renders the final video from a cuesheet via ffmpeg commands",
  "type": "module",
  "main": "./dist/plan.js",
  "types": "./dist/plan.d.ts",
  "bin": {
    "cuesheet-render": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cuesheet/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
