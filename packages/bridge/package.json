{
  "name": "@cuesheet/bridge",
  "version": "0.0.0",
  "description": "MCP bridge that Claude Code connects to for editing cuesheets (natural-language commands -> validated cuesheet changes)",
  "type": "module",
  "bin": {
    "cuesheet-bridge": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cuesheet/schema": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
