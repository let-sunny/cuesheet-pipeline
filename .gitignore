node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/

# E2E (Playwright) - fixture media/working cuesheet copy are generated by globalSetup, plus
# Playwright's own report/artifact/browser-mode-screenshot dirs.
tests/e2e/.runtime/
playwright-report/
test-results/
apps/web/__screenshots__/

# large files/generated artifacts (not committed to the repo)
media/clips/*.mp4
media/clips/*.m4a
media/dotmix_src
media/proxies/
media/.thumbs/
media/title-cache/
media/out.mp4
out.mp4
out/
proto_draft_*.mp4
nohup.out
.claude/scheduled_tasks.lock
media/drafts/frames/
proto_final_*.mp4

# pipeline judgment/scan artifacts - personal data, never commit
media/drafts/

# derived outputs - never commit
*.srt

# real-episode working cuesheets - personal content, keep local only
episodes/
proto_*.cuesheet.json
archive/proto-v3/*.cuesheet.json
project.cuesheet.json
archive/

# active-episode pointer - points at a personal episode path, machine-local
.active-episode

# active-domain pointer - selects the active domain bundle, machine-local
.active-domain

# domain personal layer - a person's own transcripts and the voice profile distilled from them
# (the theme files in domains/<name>/ are committed; the personal voice is not)
domains/*/transcripts/
domains/*/voice.generated.md
